# === Server URLs ===
# Локалка: http://localhost:3000
# Preview/Prod: задаются в настройках проекта на Vercel
NEXTAUTH_URL=

# === Auth ===
# Генерировать: `openssl rand -base64 32` (или через Node, см. ниже)
NEXTAUTH_SECRET=

# === Database ===
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public
# Пример локально (Docker): postgresql://dev:dev@localhost:5432/kindinout?schema=public
DATABASE_URL=

# === Observability (включим позже) ===
SENTRY_DSN=

# === Uploads (включим позже) ===
UPLOADTHING_TOKEN=

# === Email (включим позже) ===
RESEND_API_KEY=

